/**
 * Result of a voting poll with decrypted tallies.
 *
 * This interface has no generic parameters, so we re-export from ecies-lib.
 */
export type { VotingPollResults } from '@digitaldefiance/ecies-lib';
